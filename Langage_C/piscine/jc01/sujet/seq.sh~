#! /bin/bash

i=0


if (($1 > $3)) && ($2 < 0) ;
then
	l=$1
	while (($l > $3));
	do
	l=$((l-1))
	echo "$l"
	done
	
	

elfil (($1 > $3)) && ($2 > 0) ;
then
	l=$1
	while (($l < $3));
	do
	l=$((l+1))
	echo "$l"
	done
	
elif (($1 = $3));
then
	echo ($1)
	exit 0

else
	exit 1
fi
