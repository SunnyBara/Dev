int my_strlen(const char str[])
{
    int i = 0;
    while ( str[i] != '\0')
    {
	i ++;
    }
    return(i);
}    

int main (int argc, char **argv)
{
    char str[]="abzdel" ;
    return(my_strlen(str));
}
